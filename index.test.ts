import { VisierUserManagement } from "./index";

describe("visier-user-management-typescript-sdk", () => {
    it("initialize client", async () => {
        const visierusermanagement = new VisierUserManagement({
        });
    });
});
